import { HostTemplateDirective } from './host-template.directive';

describe('HostTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new HostTemplateDirective(null);
    expect(directive).toBeTruthy();
  });
});
